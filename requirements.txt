# Flight-critical dependencies only. Do NOT add vision/ML packages here --
# they must never be required for the flight process to start on the
# Raspberry Pi. See requirements-vision.txt for the offline analysis stack.

# Pinned: MAVSDK's Battery.remaining_percent contract changed between major
# versions (0.0-1.0 fraction in 2.x, 0-100 percent in 3.x). The application's
# battery thresholds assume 3.x. Do not float this dependency.
mavsdk==3.17.2

pytest==8.3.5
pytest-asyncio==0.24.0
